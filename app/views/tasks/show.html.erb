<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p><%="completed? #{@task.completed}" %></p>

<%= link_to "Update the task", task_edit_path %>

<%= link_to "destroy", task_path(@task), data: { turbo_method: :delete, turbo_confirm: "Vous êtes sur? " } %>

<%= link_to "Back", tasks_path %>
